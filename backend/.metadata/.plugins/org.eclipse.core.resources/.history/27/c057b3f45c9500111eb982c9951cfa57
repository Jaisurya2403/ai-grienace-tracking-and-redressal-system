server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: ${ORACLE_URL:jdbc:h2:mem:authdb;DB_CLOSE_DELAY=-1;MODE=Oracle}
    driver-class-name: ${ORACLE_DRIVER:org.h2.Driver}
    username: ${ORACLE_USER:sa}
    password: ${ORACLE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: MyComplaintPortalSuperSecretKeyForJWTSigning1234567890SignatureSecure
  expiration-ms: 86400000 # 24 Hours

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: alonewarrior123456@gmail.com
    password: wpce mazd svlf jpoc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
