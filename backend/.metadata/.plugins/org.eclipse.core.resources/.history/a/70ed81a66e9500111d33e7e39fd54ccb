server:
  port: 8083

spring:
  application:
    name: complaint-service
  datasource:
    url: ${ORACLE_URL:jdbc:oracle:thin:@localhost:1521:xe}
    driver-class-name: oracle.jdbc.OracleDriver
    username: ${ORACLE_USER:complaint_db}
    password: ${ORACLE_PASSWORD:complaintpassword}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: alonewarrior123456@gmail.com
    password: wpce mazd svlf jpoc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
